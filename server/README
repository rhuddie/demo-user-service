1) Create conda env:
conda create --name demo-server python=3.6

2) activate
source activate demo-server

3) Install requirements
(demo-server) $ pip install -r requirements.txt 

python3 server/server.py --setup

curl 'http://127.0.0.1:5000/api/add' --data 'username=chip8&email=biff%40hotmail.com67&dob=123456&address=12312313'
curl http://127.0.0.1:5000/api/list

python3 -m pytest ./server/tests/test_rest_api.py


https://docs.docker.com/get-started/part2/#build-the-app

BUILD DOCKER IMAGE:
/demo-user-service/$ docker build -t demo-server .

BUILD TESTS DOCKER IMAGE:
docker build -t demo-server-tests -f Dockerfile.tests .

RUN THE SERVER:
demo-user-service/$ sudo docker run --name demo_server -p 5000:5000 demo-server

START BEHAVE TESTS:
sudo docker exec -it demo_server behave server/tests/ui/features
DELETE NAMED CONTAINER
sudo docker rm /demo_server

RUN PYTESTS:
sudo docker exec -it demo_server pytest server/tests

OPEN SHELL IN RUNNING CONTAINER:
sudo docker exec -it demo_server bash

OPEN A CONTAINER AND GET A SHELL
docker run --entrypoint "/bin/bash" -ti --name demo_server_tests -p 5000:5000 demo-server-tests


COPY LOCAL FILES INTO RUNNING CONTAINER
docker ps
[GET ID]
demo-user-service$ docker cp . [CONTAINER ID]:/demo-server/


START SERVICE AND TESTS RUNNING
docker-compose up

STOP SERVICE AND TESTS RUNNING
docker-compose down


https://docs.docker.com/get-started/part3/#about-services
